import { Component } from "react";
import {View, Text} from "react-native";

export default class Exemplo extends Component {
    render() {
        return(
            <View>
                <Text>Essa é uma classe.</Text>
            </View>
        )
    }
}